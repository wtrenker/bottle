{% extends "Base.jinja2" %}
{% block content %}
    <form action = {{ url("pick") }} method="post">
    {{ form.date.label }} {{ form.date(id="datepicker", autocomplete="off" ) }}
     {{ form.submit }}
    <a href="{{ url("admin") }}">Admin</a>
    </form>
{% endblock %}
